/*
Name:-Deal Term Validation
Description:- To validate Deal Term for less then 2 months in case of Symantec Trial/POC Quotes.
Jira Id:- Symint-3206
Developer:- Arjun/Rohit

*/

//Initialization
Dealterm=0;

//Converting string variable into integer
if(isnumber(dealTerm_t))
{
 Dealterm = atof(dealTerm_t);
}
//Adding the logic to throw the validation if Deal term is > 2 months
//Modified the condition for Deal Type = Trial POC for SYMINT-3131
if(Dealterm>2.0 and symantecTrialPOC_t and dealType_t=="trialPOC")
{
 return true;
}
else
{
 return false;
}