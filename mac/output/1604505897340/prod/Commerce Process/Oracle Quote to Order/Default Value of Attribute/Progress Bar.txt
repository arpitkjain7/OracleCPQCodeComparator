/* Added by Sindhura */
htmlStr = "";
classType = "";
currentStep = "";
index = 0;

company = _system_company_name;

//css path
cssStyle = "<link rel=\"stylesheet\" href=\"$BASE_PATH$/images/BreadcrumbStyle.css\">";

//The Text to display inside the breadcrumb
stepStr = string[];

//breadcrumb steps
breadcrumbMainSteps = string[]{"DRAFT","IN_PROGRESS","PENDING_APPROVER","APPROVED","REJECTED","ORDER_SUBMITTED"};



//GET THE CURRENT STEP/STATUS
currentStep = status_t;

htmlStr = "<div class=\"breadcrumb flat\">";
htmlStr = htmlStr  + "<link rel=\"stylesheet\" type=\"text/css\" afrres=\"true\" href=\"$BASE_PATH$/images/BreadcrumbStyle.css\"/>";

for step in breadcrumbMainSteps {
    if(step == currentStep) {
  if(findinarray(breadcrumbMainSteps,currentStep) == sizeofarray(breadcrumbMainSteps) - 1){
   classType = "visited";
  } else {
   classType = "active";
  }
 } else {
  if(index < findinarray(breadcrumbMainSteps, currentStep)) {
      classType = "visited";
  } else {
      classType = "";
  }
 }
 if(step=="IN_PROGRESS")
 {
 step="IN PROGRESS";
 }
 if(step=="PENDING_APPROVER")
 {
 step="PENDING APPROVAL";
 }
 if(step=="ORDER_SUBMITTED")
 {
 step="ORDER SUBMITTED";
 }
 if(step=="DRAFT")
 {
 step="CREATED";
 }
 htmlStr = htmlStr + "<a href=\"#\" class=\"" + classType + "\"> "+step+"</a>";    
    index = index + 1;

}
htmlStr = htmlStr + "</div>";
htmlStr = "<div>"+cssStyle + htmlStr+"</div>";

return htmlStr;