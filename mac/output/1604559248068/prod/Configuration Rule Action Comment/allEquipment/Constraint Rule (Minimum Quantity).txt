mpn=PartNumber_avg;
qty=quantity_avg;
ret="";
minimumQuantityString="";
/*res1 = bmql("SELECT Minimum FROM BRCM_Part_Master_Ext WHERE MPN = $mpn");
for each in res1
{
 minimumQuantityString=get(each,"Minimum");
}
if(minimumQuantityString <> "" and transactionsType_avg== "New" )
{
 
 minimumQuantityInt = atoi(minimumQuantityString);
 
 if( qty < minimumQuantityInt)
 {
  ret="Quantity for Product Number "+PartNumber_avg+" should have Quantity greater than or equal to "+minimumQuantityString ;
 }
} */
return ret;