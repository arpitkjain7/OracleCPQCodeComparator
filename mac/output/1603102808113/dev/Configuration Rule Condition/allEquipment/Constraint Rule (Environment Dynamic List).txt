/*****************************************************************
Function Name : Environment Dynamic List
Name : Nandhini Vurimella
Description : environment list
*****************************************************************/
if(partAttributeExtFetched_avg <> "") 
{
 partJson=json(partAttributeExtFetched_avg);
 productType = jsonget(partJson,"Product Type");
 environmentAllowed = trim(upper(jsonget(partJson,"Environment Allowed")));
 //prodType = str[0];
 if(productType == "Software" and environmentAllowed <> "Y" and selectedProductNumberInt_avg <> "" and transactionsType_avg <> "")
 {
  return true;
 }
 else
 {
  return false;
 }
}
return false;