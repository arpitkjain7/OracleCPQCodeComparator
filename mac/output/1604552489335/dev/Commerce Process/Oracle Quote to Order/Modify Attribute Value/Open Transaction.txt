/************************************************************************************************************
Description: This function is used to set the flag value
Return type: string
History:     Date          Author                     Comment 
            18/09/2020    Sindhura Chintakindi        Added this as part of JIRA-353
             23/10/2020    Sindhura Chintakindi       Ignore dropped products Jira-962
**************************************************************************************************************/ 

retVal=false;
if(renewalquoteflag_t == false)
 {
 if(quoteType_t == "renewalQuote" AND (status_t <> "APPROVED" OR status_t <> "SUBMITTED"))
  {
  for line in transactionLine
   {
    if(line.transactionTypeM_l<>"" AND line.dropProduct_l<>true)
    {
      if(line.transactionTypeM_l == "Renew" OR line.transactionTypeM_l == "Capacity" OR line.transactionTypeM_l == "Upgrade" )
       {
       retVal=false;
       break;
       }
      else
            {
            retval=true;
            }

    }
   }
  }
  return retVal;
 }
else
{
return renewalquoteflag_t;
}