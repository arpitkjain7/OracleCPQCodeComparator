/*****************************************************************
Function Name : Hide Contact Array Set
Name :Satyabrata Acharya
Description : This Rule used to hide contact array
*****************************************************************/ 
result=false;
size=range(customersArrayControl_avg);
counter=0;
count="0";
//loop to check which customer is selected
for each in size{
if(selectCustomer_t[each]==true){
counter=counter+1;

}

}
//condition to check if contact detail string is not null then find the count
if(contactDetailsJsonString_avg<>""){
jsonObj=json(contactDetailsJsonString_avg);
count=jsonget(jsonObj,"count");
}
// when both the above condition satisfies rule will fire
if(counter<=1 AND (atoi(count)==0)){
result=true;
}
return result;