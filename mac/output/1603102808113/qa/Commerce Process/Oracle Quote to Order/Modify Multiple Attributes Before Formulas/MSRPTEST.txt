lineNumber = string[];
final = string[];
dataSet = bmql("select QUOTE_LINE_NUMBER from unitMsrpData where TRANSACTION_ID = $bs_id");
for each in dataSet{
 append(lineNumber,get(each,"QUOTE_LINE_NUMBER"));
}
for line in transactionLine{
 if(findinarray(lineNumber,string(line._sequence_number))<> -1){
 append(final,line._document_number+"~extMSRP_l~"+string(line.unitMSRP_l*line._price_quantity));
 }
}
return join(final,"|");