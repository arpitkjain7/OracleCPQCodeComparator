/***********************************************************
Function Name : validation on Drop Product if Drop reason is not specified
Name : Sindhura Chintakindi
Description :  validation on Drop Product selection if Drop reason is not specified
**********************************************************/
if(dropProduct_l== true)
{
 if(dropReason_l == "")
 {
  return true;
 }
}
return false;