finalStr = "";
maintenanceRequiredFlag = "";
if(partAttributesFetched_avg <> "" )
{
 partJson = json(partAttributesFetched_avg);
 //print str[1];
 //supportType = str[1];
 support = jsonget(partJson,"Support Type");
 splitString = split(support,"|");
 for each in splitString
 {
  finalStr = finalStr + each + "|^|" ;
 }
 maintenanceRequiredFlag = lower(trim(jsonget(partJson,"Maintenance Required")));
 /*if(maintenanceRequiredFlag == "y")
 {
  finalStr = finalStr + "Standard Support|^|" ;
 }*/
 print finalStr;
}
return finalStr;