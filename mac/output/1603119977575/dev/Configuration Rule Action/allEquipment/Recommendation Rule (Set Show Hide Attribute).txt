/*
This rule will set select button
Created by shruti on 20th dec 2018

*/
retStr = Boolean[];

for iLoop in select_avg{

  if(hideButton_avg==true){
  append(retStr,false);
  }
  else{
  append(retStr,iLoop);
  }
}
return retStr;
/*
This rule will set show hide attribute
Created by shruti on 20th dec 2018 

*/
retStr = "HIDE";

for iLoop in select_avg{
  if(iLoop == true){
    retStr = "SHOW";
    break;
  }
}
//Added Code By Rohit Ranjan For SYMINT-3144 
if(selectUpgradeProduct_avg==true){
for select in selectUpgdSKUarr_avg{
  if(select == true){
    retStr = "SHOW";
    break;
    }
 }
}
//Code Ends for SYMINT-3144
if(hideButton_avg==true){
 retStr = "HIDE";
}

return retStr;
/*
This rule will set select button for upgrade SKU
Created by Rohit Ranjan on 16th Jan 2019 for SYMINT-3144

*/
//Added Code By Rohit Ranjan For SYMINT-3144 
retStr=boolean[];
if(selectUpgradeProduct_avg==true)
{
 for select in selectUpgdSKUarr_avg{

  if(hideButton_avg==true){
  append(retStr,false);
  }
  else{
  append(retStr,select);
    }
 }
} 
return retStr;